import React from 'react'; //importa o react
import {Text} from 'react-native'; //importa o componente Text do react-native

export default () => {
  return <Text>Meu primeiro componente</Text>; // Note que podemos tratar a tag <Text> como se fosse um código JS
};
